import { UnisearchfilterPipe } from './unisearchfilter.pipe';

describe('UnisearchfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new UnisearchfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
